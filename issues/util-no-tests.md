# Zero unit tests for util module

## Component
`src/util/`

## Severity
Low

## Description

No unit tests exist for any file in the util module:
- `mod.rs` — exit codes, `now_secs()`
- `error.rs` — LockError type
- `signals.rs` — signal handler setup, `should_exit()`
