# $@ expansion in program arguments not implemented

Procmail expands `$@` to the recipients passed on the command line (from `-d`
or the tail arguments). This is not implemented in variable substitution.
