# Formail module cleanup

## Component
`src/formail/`

## Severity
Low

## Description

The formail module has only a single submodule called `fields`.  Move the
`fields` module up a level.

Ensure that all pub functions are documented.

Ensure that all functions are tested, as a few are missing tests.

